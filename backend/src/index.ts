// index.ts
import './server';  // Import the main server file

// You could also add additional initialization logic here if needed in the future,
// like connecting to a database, logging setup, etc.
